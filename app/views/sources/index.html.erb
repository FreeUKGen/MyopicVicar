<% if session[:manage_user_origin] == 'manage syndicate' %>
  <% if session[:my_own_list] == 'county' %>
    <% breadcrumb :syndicate_available_groups_by_county_image_server_group, @county, @place, @church, @register, @source, @group %>
  <% else %>
  	<% breadcrumb :syndicate_sources, @county, @place, @church, @register %>
  <% end %>
<% else %>
  <% if session[:my_own_list] == 'county' %>
    <% breadcrumb :county_available_groups_by_county_image_server_group, @county, @place, @church, @register, @source, @group %>
  <% else %>
    <% case session[:ig_allocation] %>
      <% when 'by_syndicate' %>
        <% breadcrumb :sort_by_syndicate_image_source, @county, @place, @church, @register, @source %>
      <% when 'by_place' %>
        <% breadcrumb :sort_by_place_image_source, @county, @place, @church, @register, @source %>
      <% when 'by_all' %>
        <% breadcrumb :sort_by_all_image_source, @county, @place, @church, @register, @source %>
      <% when 'by_unallocate' %>
        <% breadcrumb :sort_by_unallocate_image_source, @county, @place, @church, @register, @source %>
      <% else %>
        <% breadcrumb :county_sources, @county, @place, @church, @register %>
    <% end %>
	<% end %>
<% end %>

<div style="clear:both;"></div><br>
<p style="text-align: center"><b>Sources</b> for <b><%= @register_type %></b> register for <b><%= @church_name %></b> in <b><%= @place_name %></b> of <b><%= @county %></b>

<% title "Source - list" %>
<% if flash[:notice] %>
  <div id="notice" style="color:blue">
   	<%= flash[:notice] %><% flash.clear %>
  </div>
<% end %>

<div class="grid">
  <section class="island">
    <%= render 'source_index' %>
  </section>
</div>