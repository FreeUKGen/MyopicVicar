<% breadcrumb :edit_freecen2_civil_parish,  @freecen2_civil_parish, @piece.chapman_code, @piece.year , @type%>
<h2 style="text-align: center">Edit FreeCen2 Civil Parish:- <%= @freecen2_civil_parish.name%> in <%=ChapmanCode.name_from_code(@chapman_code)%> (<%=@chapman_code%>) for <%=@piece.year%></h2>
<%= render 'flash_notice' %>
<h3 style="text-align: center"> Name changes will normally be performed using the Edit Civil Parish Name action and only extremely rarely using the main Edit action</h3>
<h4 style="text-align: center"> Before changing the Place to match an existing Place consider if it would be better to add a new Place with this name and appropriate geographic coordinates</h4>
<%= render 'form' %>
<br>