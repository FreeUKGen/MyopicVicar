<% breadcrumb :edit_freecen_csv_file,  @freecen_csv_file%>
<p style="text-align: center"><b>Editing  file :<b><%= file_name(@freecen_csv_file) %> (<%= userid(@freecen_csv_file) %>) in <b><%= @piece.district_name %></b> of <b><%= @piece.district_chapman_code %></b><br>
    </p>
    <%= render partial: 'flash_notice' %>
    <%unless session[:error_line].nil?%>
      <b>If the header error involves an incorrect record type (BA,BU or MA) the file itself must be corrected and reloaded</b><br>
      <br>
      <%n = 0%>
      <%while n < @n %>
        <b><%=@error_message[n]%><%= @content[n]%></b>
        <br>
        <%n = n + 1%>
      <%end%>
    <%end%>
    <br>
    <%= render 'form' %>
    <br>