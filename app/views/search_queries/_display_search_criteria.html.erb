<% search_criteria = appname.downcase == 'freebmd' ? bmd_search_criteria(@search_query) : search_params_for_display(@search_query) %>
<% if defined?(@printable_format) && @printable_format %>
  <% search_criteria.each do |name, value| %>
    <%= name %>: <b><%= value %></b> ;
  <% end %><br>
  <%= pluralize(@result_count, ' Search Result', ' Search Results') %>
<% else %>
  
    <!--div class="grid__item  one-sixth  lap-one-sixth  palm-one-whole push-half--top">
      <div class="alpha results_block flush--bottom push-half--right">
          Results
        </div>
        <div class='result_count'>
          <%= pluralize(@result_count, 'Result', ' Search Results') %>
        </div>
    </div-->
    <div class='container'>
      <div class="grid">
        <div class="grid__item  seven-tenths lap-three-quarters palm-one-whole">
          <section class="islet islet--light soft--left hard--bottom">
            <div class='grid'>
              <div class="grid__item one-whole lap-one-whole palm-one-whole">
                <h2 class="epsilon flush--bottom push-half--bottom"><em>When you searched for:</em></h2>
                <div class="criteria-group">
                  <% search_criteria = appname.downcase == 'freebmd' ? bmd_search_criteria(@search_query) : search_params_for_display(@search_query) %>
                  <% search_criteria.each do |name, value| %>
                    <dl class="criteria">
                      <dt class="criteria__name epsilon"><%= name %></dt>
                      <dd class="criteria__value zeta"><%= value %></dd>
                    </dl>
                  <% end %>
                </div>
              </div>
              <%# if appname.downcase == 'freebmd' %>
                <!--div class="grid__item one-whole lap-one-whole palm-one-whole text--right">
                  <%#= link_to "Revise Search", new_search_query_path(:search_id => @search_query), :class => "btn   btn--small " %>
                  <%#= link_to "New Search", new_search_query_path, :class => "btn   btn--small  " %>
                </div-->
              <%# end %>
            </div>
          </section>
        </div>
      </div>
    </div>
<% end %>
    <!--div class="grid__item  one-quarter lap-one-quarter  palm-one-whole  push-half--top">
      <h2 class="gamma flush--bottom"> We found <%#= pluralize(@result_count, ' Result', ' Results') %></h2>
      <%# unless defined?(@printable_format) && @printable_format%>
        <small class="additional zeta">Results are presented by ascending date. Clicking a column heading sorts by that column. Another click reverses the order.<br>
          Each View button in the <q>Detail</q> column opens a page with all the information we have for that record.</small>
      <%# end %>
    </div-->
  

