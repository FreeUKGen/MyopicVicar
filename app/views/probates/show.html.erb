<%= render( :partial => "nav_freeprobate", :locals => {search_query: params[:search_query], search_record: @search_record}) %>
<div class="grid">
  <div class="grid__item seven-tenths lap-one-whole palm-one-whole ">
    <h1 class="beta push-half--top flush--bottom"><%= @event_type %> detail for <%= @death[:GivenName] %> <%= @death[:LastName] %></h1>
    <section>
      <%= render( :partial => "show_freepro_record", :locals => {search_record: @search_record, death: @death, event: @event} ) %>
    </section>
  </div>
</div>
