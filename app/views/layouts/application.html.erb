<html>
    <head>
	<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
	<title></title>

	<!-- Styles -->

	<link href='http://fonts.googleapis.com/css?family=Corben' rel='stylesheet' type='text/css'>

	<%= stylesheet_link_tag "normalize" %>
	<%= stylesheet_link_tag "layout" %>
	<%= stylesheet_link_tag "freereg" %>
   <%= javascript_include_tag "application" %>
	<%= javascript_include_tag "jquery.min.js" if controller.controller_name == "search_queries"%>
	<%= javascript_include_tag "jquery.chained.remote.js" if controller.controller_name == "search_queries"%>

    </head>
    <body>
    <div id="container" > <h2 id='logo' style="text-align: center"> <%= link_to Refinery::Core.site_name, refinery.root_path %></h2></div>
  <div id="navhead"><br>
 <h4><div style="float:left;"> <%= button_to " Search Our Database", main_app.search_queries_path , method: :get%></div>
 <div style="float:left;"><%= button_to 'Advice on Searching', "../tips-for-searching-the-database" , method: :get  %></div>
 <div style="float:left;"> <%= button_to " County and Parish Content", refinery.county_pages_county_pages_path , method: :get%></div>
 <% if current_refinery_user %>
  <div  style="float:left;"> <%= button_to 'Resource Management', main_app.manage_resource_path(current_refinery_user.userid_detail), method: :get %> </div>
 
   <div style="float:left;"> <%= button_to" Logout", refinery.logout_path , method: :get%></div>
 <% else %>
   <div style="float:left;"> <%= button_to" Login for members", refinery.login_path, method: :get %></div>
    <div style="float:left;"> <%= button_to" Registration", main_app.general_userid_detail_path, method: :get %></div>
   <% end %> 
  <div style="float:left;">  <%= button_to_function "Previous Page", "history.back()" %> </div>

</h4>
</div>
<%if controller.controller_name == "freereg_contents"  %>
<div id="Heading">Please use these pages to review information on the data held in the FreeREG database on the various Places, Churches and Registers in England, Scotland and Wales. They do NOT display the data itself; just information about the data, including the years for which data exists in the database and when it was last updated on <%= Rails.application.config.dataset_date %>. </div>
<%end%>
<%unless @first_name.nil?%>
<div id="personal">
<p style="text-align: center"><b><%= @first_name%> these are your management tools</b></p>
</div>

<%end%>
 <div id="content">
			<%= yield %>
 </div>

<div id = "footer">
<div style="float:left;"> <%= button_to " About Us", "../about"  , method: :get%></div>
 <div style="float:left;"><%= button_to 'Contact Us', "../freereg2/contact" , method: :get  %></div>
  
 <% if current_refinery_user %>
	  <div  style="float:left;"> 
	  	<%= form_tag(problem_url, :method=>:get) do %>
	  		<% problem_button_options.each_pair do |name, value| %>
	  			<%= hidden_field_tag(name, value)%>
	  		<% end %>
	  		<input type="submit" value="Report a Problem" />
	  	<% end %>
  	  </div>
  <% end %>
 <div style="clear:both;"> </div><br>
<small>© 1998-2014 The website, its layout, search engine, and database are copyright by the Trustees of Free UK Genealogy, a charity registered in England and Wales, Number 1096940.We make no warranty whatsoever as to the accuracy or completeness of the FreeREG data. Use of the FreeREG website is conditional upon acceptance of the <%= link_to "Terms and Conditions", "../freereg2/terms-and-conditions" %>
 </small><div>
    </body>
</html>
