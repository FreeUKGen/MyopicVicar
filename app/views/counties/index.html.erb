Page Identification Counties:index<p>

<div style="float:left;"> <%= button_to 'Create a new county', new_county_path, method: :get %> </div>

<div  style="float:left;"> <%= button_to 'Back to my Resource Management', manage_resource_path(@user), method: :get %> </div>
<div style="float:left;">  <%= button_to_function "Previous Page", "history.back()" %> </div>
<div style="float:left;"> <%= button_to" Logout", refinery.logout_path , method: :get%></div>
<div style="clear:both;"> </div><br>
  <% if flash[:notice] %>
    <div id="notice" style="color:blue"><br>
    <%= flash[:notice] %> <br>
    </div>
   <% end %>  

<div id="main"style="color:black">
<table  CLASS="boldtable" style="width: 100%">
  <tr>
    
    <th style="width:25%">County</th>
    <th style="width:25">County Coordinator</th>
     <th style="width:25%">Show</th>
    <th style="width:25%">Edit</th>
  </tr>

<% @counties.each do |county| %>
  <tr id="<%=county.id%>">
   <td > <%= county.chapman_code %>  </td>
   <td > <%= county.county_coordinator %>  </td>   
   
   
   
     <td><%= button_to 'Show', county_path(county), method: :get  %></td><td><%= button_to 'Edit details', edit_county_path(county), method: :get  %>
  </tr>
  <% end %>
  
</table>
</div>

<div style="float:left;"> <%= button_to 'Create a new county', new_county_path, method: :get %> </div>

<div  style="float:left;"> <%= button_to 'Back to my Resource Management', manage_resource_path(@user), method: :get %> </div>
<div style="float:left;">  <%= button_to_function "Previous Page", "history.back()" %> </div>
<div style="float:left;"> <%= button_to" Logout", refinery.logout_path , method: :get%></div>
<div style="clear:both;"> </div><br>

