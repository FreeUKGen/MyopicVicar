
<p>This table contains a listing of those Places and Churches for <b> <%= @county  %></b> for which we have searchable data. Select the Genuki link where it exists to find out the actual location of the Place. Select the Church link to obtain additional information about the specific registers that have been transcribed for that church.
 
</p>
<div id="nav">
	<%= button_to 'Back to County Search Page', freereg_contents_path , method: :get%>
</div>
<table>
<tr>
    <th >Place</th>
    <th >Alternate place name</th>
    <th >Genuki page</th>
    <th >Last amended</th>
    <th >Churches</th>
</tr>


<% @search_results.each do |place|%>
<tr id="<%=place.id%>">
 <td >
     <%= place.place_name %>
    </td>
       <td >
     <%= place.alternate_place_name %>
      </td>
  <td >
     <%= link_to('Genuki', place.genuki_url) unless place.genuki_url.nil?%>
      </td>
     <td >
     <%= place.last_amended %>
      </td>
    
  <td><ul>
	  <% place.churches.order_by(:church_name.asc).each do |church| %>
	  	<li>
		  	<%= link_to(church.church_name, show_church_path(church)) %>
			<%= church.denomination %>	
			<%= church.location %>	<br />
	  	</li>
	  <% end %>  	
  </ul>
</td>
</tr>

<% end %> 


</table>
<div id="nav">
	<ul>
	    <li><%= button_to 'Back to County Search Page', freereg_contents_path, method: :get  %>
	    	
    	</li>
    	<% if @page_number > 0 %>		    	
		    <li>
		    	<%= link_to "Previous", freereg_content_path(@search_query, :page_number => @page_number-1) %>
	    	</li>
    	<% end %>
	</ul>
</div>


