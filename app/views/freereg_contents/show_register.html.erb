<% require 'record_type' %>

<p></p>
<p>Details of the Register <b><%= @register_name %></b> for 
<b><%= @place_name %>;</b>
  
  <b><%= @church %></b>
   
   in the County of:
   <b><%= @county %></b>
</p>
In this Table the Register Type indicates the source used for the transcription and can be: PR is from a Parish Register; AT is from an Archdeacon's Transcript; BT is from a Bishop's Transcript; TR is from a transcript of the Parish Registers; PH is from a Phillimore Transcript of a Parish Register; EX is from a selective extract of the Parish Register; DW is from a Dwelly's Transcript of a Parish register; and if there is no entry our transcription is from an unspecified source but is most likely transcribed from a Parish Register. 
<p>You may see the decade for which registers have been transcribed by following the "Decadal Data" link.</p>
<table>
  <tr>
    <th style="width:10%">Record type</th>
    <th style="width:10%">Number of records</th>
    <th style="width:7%">First year</th>
    <th style="width:7%">Last year</th>
    <th style="width:34%">Credit</th>
    <th style="width:10%">Transcription Date</th>
    <th style="width:10%">Last amended</th>
    <th style="width:12%">Link to Decadal data</th>
   
  </tr>

<% @files.each do |file| %>
  <tr> 
   <% value = RecordType.display_name(file.record_type) %>
    <td style="width:10%"><%= value %></td>
    <td style="width:10%"><%= file.records %></td>
    <td style="width:7%"><%= file.datemin %></td>
    <td style="width:7%"><%= file.datemax %></td>
    <% if file.credit_name.nil? %> 
    <td style="width:34%"><%= file.transcriber_name%></td>
    <%end%>
     <% unless file.credit_name.nil? %> 
    <td style="width:34%"><%= file.credit_name %></td>
    <%end%>
    <td style="width:10%"><%= file.transcription_date %></td>
    <td style="width:10%"><%= file.modification_date %></td>
    <% unless file.records.nil? %>
    <td style="width:12%"><%= link_to "Decadal Data", show_decade_path(value) %></td>
    <%end%>
    
  </tr>
<% end %>
</table>
    <%= button_to_function "Back to Previous Page", "history.back()" %> <br>
    <%= button_to 'Back to Places Page', show_freereg_content_path(@search_query,:page_number => @page_number, :anchor => "#{@place}"), method: :get %> 