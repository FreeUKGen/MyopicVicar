<p>Editing an entry in <b><%= @county %></b><br></p>

<%= render 'form' %>

<%= button_to_function "Back to Previous Page", "history.back()" %> 
<%= button_to 'Back to Listing of Places', master_place_names_path, method: :get %>
