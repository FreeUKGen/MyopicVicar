<% require 'record_type' %>
<% if flash[:notice] %>
    <div id="notice"><%= flash[:notice] %></div>
<% end %>
<p></p>
<p>
  Church name:
  <b><%= @church.church_name %></b>
  for Place: 
   <b><%= @place_name %></b>
   in the County of:
   <b><%= @county %></b>
</p>

<table style="width: 100%">
  <tr>
  
    <th>Other church name</th>
    <th>Denomination</th>
    <th>Location</th>
    <th>Last amended</th>
    <th>Notes</th>
    <th>Register Names</th>
    <th>Alternate register names</th>
    <th>Action</th>
    
  </tr>


  <tr>
 
  <td><%= @church.alternate_church_name %></td>
  <td><%= @church.denomination %></td>
  <td><%= @church.location %></td>
  <td><%= @church.last_amended %></td>
  <td><%= @church.church_notes %></td>
  
  <td><ul>
    <% @church.registers.each do |register| %>
   <li>
    <%= register.register_name %>
   
   
   </li> 
  
<% end %>
</ul></td>
<td><ol>
 <% @church.registers.each do |register| %>
   <li>
<%= link_to(register.alternate_register_name, register_path(register)) %>
</li> 
  
<% end %>
</ol></td>

  <td >
     
<%= link_to 'Edit Church', edit_church_path(@church) %>
<%= link_to 'Destroy Church', church_path(@church), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
</table>
<br>

<%= button_to_function "Back to Previous Page", "history.back()" %> 
