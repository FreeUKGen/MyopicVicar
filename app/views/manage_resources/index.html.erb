<p style="text-align: center"><b>From here you can manage all of your FreeREG resources</b></p>
<div id="nav">
  <div style="float:left;"> <%= button_to 'My Saved Searches', my_saved_searches_path, method: :get  %> </div>
  <div style="float:left;"><%= button_to 'My Profile',  my_own_userid_detail_path, method: :get  %></div>
  <div style="float:left;"><%= button_to 'My Own Files', my_own_freereg1_csv_file_path, method: :get if can_manage_files? %></div>
  <% unless @user.syndicate_groups.nil?  %>
    <div style="float:left;"><%= button_to 'Manage My Syndicate(s)', manage_syndicates_path, method: :get if can_manage_syndicates?(@user)  %></div>
  <%end%>
  <% unless  @user.county_groups.nil? %>
    <div style="float:left;"><%= button_to "Manage County Data ", manage_counties_path, method: :get  if can_manage_county_data?(@user) %></div>
  <%end%>
  <div style="float:left;"><%= button_to 'Manage FreeREG' , manage_freeregs_path, method: :get  if can_manage_freereg?(@user) %>
  </div>
</div>
<div style="clear:both;"> </div><br>
</div>