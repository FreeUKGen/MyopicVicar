Page Identification Manage Resources:show<p>
<p style="text-align: center"><b>From here you can manage all of your FreeREG resources</b></p>
<div id="nav">

<div style="float:left;"> <%= button_to 'My Saved Searches', my_saved_searches_path, method: :get  %> </div>
<div style="float:left;"><%= button_to 'My Profile',  my_own_userid_detail_path, method: :get  %></div>


<div style="float:left;"><%= button_to 'My Own Files', my_own_freereg1_csv_file_path, method: :get unless @user.person_role == "researcher"  %></div>
<% unless @user.syndicate_groups.nil?  %>

<div style="float:left;"><%= button_to 'Manage My Syndicate(s)', manage_syndicates_path, method: :get if @user.person_role == 'syndicate_coordinator' || @user.person_role == 'volunteer_coordinator' || @user.syndicate_groups.length > 0  %></div>
<%end%>
<% unless  @user.county_groups.nil? %>
<div style="float:left;"><%= button_to "Manage County Data ", manage_counties_path, method: :get  if @user.person_role == 'county_coordinator' || @user.person_role == 'country_coordinator' || @user.person_role == 'data_manager' || @user.county_groups.length > 0 %></div>

<%end%>

<div style="float:left;"><%= button_to 'Manage FreeREG' , manage_freeregs_path, method: :get if @user.person_role  == 'system_administrator' || @user.person_role  == 'technical' %></div>

<div style="float:left;">  <%= button_to_function "Previous Page", "history.back()" %> </div>
<div style="float:left;"> <%= button_to" Logout", refinery.logout_path , method: :get%></div>
</div>

<div style="clear:both;"> </div><br>
</div>