defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: mv-development

test:
  <<: *defaults
  database: mv-test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: mv
  username: <%= ENV['MONGO_USERNAME'] %>
  password: <%= ENV['MONGO_PASSWORD'] %>
