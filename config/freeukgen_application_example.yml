development:
  template_set: 'freecen'
  # Set master: '1' on the master server; leave blank on replicas
  master:
  # URL of the FreeBMD master server (e.g. https://www.freebmd.org.uk); leave blank for single-server
  freebmd_master_url:
  advert_key:
    data_ad_client:
    data_ad_slot_header:
    data_ad_slot_google_advert:
    data_ad_slot_coverage:
    data_ad_slot_fullwidth:

test:
  template_set: 'freecen'
  master:
  freebmd_master_url:
  advert_key:
    data_ad_client:
    data_ad_slot_header:
    data_ad_slot_google_advert:
    data_ad_slot_coverage:
    data_ad_slot_fullwidth:

production:
  template_set: 'freecen'
  master:
  freebmd_master_url:
  advert_key:
    data_ad_client:
    data_ad_slot_header:
    data_ad_slot_google_advert:
    data_ad_slot_coverage:
    data_ad_slot_fullwidth:

# valid options are freecen, freereg, freebmd others can be added into application.rb

