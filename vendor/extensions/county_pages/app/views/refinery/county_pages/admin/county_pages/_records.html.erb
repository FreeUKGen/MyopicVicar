<%= render 'refinery/admin/search_header', :url => refinery.county_pages_admin_county_pages_path %>
<div class='pagination_container'>
  <% if @county_pages.any? %>
    <%= render 'county_pages' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
