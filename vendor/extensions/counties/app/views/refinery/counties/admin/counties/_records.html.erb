<%= render 'refinery/admin/search_header', :url => refinery.counties_admin_counties_path %>
<div class='pagination_container'>
  <% if @counties.any? %>
    <%= render 'counties' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
