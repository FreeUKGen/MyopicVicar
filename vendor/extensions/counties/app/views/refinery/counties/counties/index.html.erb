<% content_for :body do %>
  <ul id="counties">
    <% @counties.each do |county| %>
      <li>
        <%= link_to county.county, refinery.counties_county_path(county) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
